import styled from 'styled-components';

export const ArtistListWrapper = styled.ul`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  margin-top: -103px;
  gap: 20px;
`;
